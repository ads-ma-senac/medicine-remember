import { Text } from "react-native-paper";
import { View } from "react-native";

export default function Reminders() {
  return (
    <View
      style={{
        flex: 1,
        margin: 16,
      }}
    >
      <Text>Reminders</Text>
    </View>
  );
}
